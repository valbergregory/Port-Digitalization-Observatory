# Ativado em todo início de sessão R na raiz do projeto.
# renv inicializado em 2026-09-07 (lockfile: renv.lock).
if (file.exists("renv/activate.R")) source("renv/activate.R")

options(
  stringsAsFactors = FALSE,
  scipen = 999,
  timeout = 300,           # downloads grandes da ANTAQ/Comex
  encoding = "UTF-8"
)

# R não está no PATH desta máquina; RStudio o localiza sozinho.
# Para jobs de terminal use: "C:\Program Files\R\R-4.4.3\bin\Rscript.exe"
