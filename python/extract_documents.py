"""Extração de texto de documentos oficiais (PDF/DOU) para datação de
intervenções. ESBOÇO — requer pymupdf/pypdf (instalar via .venv quando a
etapa ativar). Nunca inventar datas: cada extração gera par (texto, fonte)."""

raise SystemExit("Etapa não ativada — instalar dependências e implementar na 2ª rodada.")
